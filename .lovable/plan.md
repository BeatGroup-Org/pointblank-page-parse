

## Sfondo nero per la sezione Team

### Cosa cambia

Due modifiche puntuali nel file `src/components/sections/TeamSection.tsx`:

1. **Sfondo sezione**: Cambiare la classe del tag `<section>` da `bg-background` a `bg-black`
2. **Titolo bianco**: Aggiungere `text-white` al tag `<h2>` del titolo "IL NOSTRO TEAM"

Tutto il resto (tile, overlay info, bottone, frecce) rimane invariato.

